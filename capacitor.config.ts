import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.EventPlanner',
  appName: 'EventPlanner',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
